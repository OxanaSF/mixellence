import React, { useState, useEffect } from "react";

//* The Modal Context Store, where we can activate the modal

const ModalContext = React.createContext({
  showModal: false,
})

export const ModalContextProvider = (props) => {
  
}